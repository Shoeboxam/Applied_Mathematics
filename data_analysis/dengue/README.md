## Setup instructions

1. install python 3.5+
2. run the package install command:
`python3 -m pip install -r requirements.txt`
3. run the script:
`python3 ./DengueTrends.py`

Full report in: `./report.pdf`